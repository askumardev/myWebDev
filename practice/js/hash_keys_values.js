const obj = {
  name: 'Alice',
  age: 30,
  city: 'New York'
};

// Extract keys and values
const keys = Object.keys(obj);
const values = Object.values(obj);

console.log('Keys:', keys);   // Output: ['name', 'age', 'city']
console.log('Values:', values);


// node practice/js/hash_keys_values.js